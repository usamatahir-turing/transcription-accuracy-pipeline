# ASR engine (pulls in torch + transformers as dependencies)
qwen-asr

# GPU torch (CUDA 12.8 build for NVIDIA Turing+ GPUs, e.g. RTX 2070).
# NOTE: plain `pip install -r requirements.txt` installs the CPU-only torch.
# To get GPU support, install torch from the CUDA index BEFORE/AFTER this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# (pick the cuXXX matching your driver; see https://pytorch.org/get-started/locally/)
torch

# Whisper - used ONLY for whisper.normalizers.BasicTextNormalizer
openai-whisper

# Scoring / metrics
jiwer

# Audio I/O and slicing
soundfile

# Data handling and report tables
numpy
pandas
tabulate
openpyxl

# Google Drive sync (download_and_upload_data.py)
google-auth
google-api-python-client
requests
